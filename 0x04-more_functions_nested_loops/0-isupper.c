#include "main.h"
/**
 * _isupper: function for printing uppercase
 * @c - variable for uppercase letters
 *
 * Return: 1 if uppercase
 * Return: 0 otherwise
 */

int _isupper(int c)
{
if (c >= 'A' &&  c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
