#include "main.h"
/**
 * _isupper: printing uppercase
 * @c - variable for uppercase letters
 * Return: int
 */

int _isupper(int c)
{
if (c >= 'A' &&  c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
