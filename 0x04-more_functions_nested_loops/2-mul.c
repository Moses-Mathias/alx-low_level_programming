#include "main.h"
/**
 * function that multiplies two integers
 * @a: first integer variable
 * @b: second integer variable
 *
 * Return: mul
 */
int mul(int a, int b)
{
int times = a * b;
return (times);
}
