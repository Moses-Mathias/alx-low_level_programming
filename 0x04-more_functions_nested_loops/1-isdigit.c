#include "main.h"
/**
 * _isdigit: function that checks for a digit
 * @c: variable to use
 *
 * Return: 1 if it is a digit
 * Return: 0 if otherwise
 */

int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
else
print (0);
}
