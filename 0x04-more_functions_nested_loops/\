#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: first integer variable
 * @b: second integer variable
 * Return: int
 */
int mul(int a, int b)
{
int times = a * b;
return (times);
}
